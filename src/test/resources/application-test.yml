spring:
  data:
    redis:
      host: localhost
      port: 6379

  datasource:
    url: jdbc:postgresql://localhost:5432/realtime_notification
    username: postgres
    password: postgres

app:
  instance:
    id: integration-test